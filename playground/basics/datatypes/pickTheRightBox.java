/*
* Difficulty: Easy–Medium | Tests: range reasoning, overflow awareness You're told a variable must hold values between 0 and 300,000,000. Choose the smallest primitive type that safely fits this range, declare it, assign the value 300000000, and print it. Then, deliberately declare a second variable of a too-small type (short) with the same value to observe what the compiler does. Input: None. Output: The correctly typed value printed; a note on the short attempt's outcome. Constraints: Justify your type choice against the actual MAX_VALUE of the type below it. Examples: 1) int fits (max ~2.1 billion) — correct choice. 2) short (max 32767) fails to compile with a literal that large — try it. 3) If the value were 3,000,000,000 instead, int would no longer be safe — identify what you'd need instead. Edge cases: Is there a difference between "the literal doesn't fit and won't compile" and "the literal fits but the computation later overflows"? What it's testing: Deliberate type selection based on range, not defaulting to int out of habit.
* */