/*
* Difficulty: Medium | Tests: conceptual limits of UTF-16 char Without writing a large program, answer: can a single Java char represent every possible Unicode character that exists? Explain what a "surrogate pair" is at a conceptual level and why it exists, and identify one practical consequence this has for someone processing certain text (e.g., emoji or rare scripts) using only single char values. Input: None. Output: A short written explanation (2–4 sentences is enough if precise). Constraints: You do not need to write code that demonstrates a surrogate pair — that requires String/array handling you haven't learned. This is reasoning-only, deliberately deferred to Strings as a topic. Examples: N/A — conceptual exercise. Edge cases: Is this a Java-specific limitation, or a consequence of char being a fixed 16-bit unit generally? What it's testing: That "char is Unicode" is only approximately true, which matters the moment you touch text outside the Basic Multilingual Plane later.
* */