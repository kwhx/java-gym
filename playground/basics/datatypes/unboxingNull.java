/*
* Difficulty: Medium | Tests: primitive vs reference type distinction, null Declare an Integer variable and assign it null. Attempt to use it in an arithmetic expression that requires unboxing to int (e.g., adding 1 to it). Predict what happens before running it. Input: None. Output: Your prediction, then the actual behavior when run. Constraints: Do not wrap this in a try/catch — you haven't learned exception handling as a topic; just observe and explain the crash. Examples: 1) Integer x = null; int y = x + 1; → throws at runtime. 2) The same code with a plain int x could never even compile to have null assigned — contrast the two. 3) Integer x = null; System.out.println(x); alone (no arithmetic) does not crash — identify why that's different from the arithmetic case. Edge cases: Why can a reference type hold null but a primitive type structurally cannot? What it's testing: Whether "primitive vs reference type" is understood as a structural distinction with real runtime consequences, not just a vocabulary pair.
* */