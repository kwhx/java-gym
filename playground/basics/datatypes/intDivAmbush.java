/*
* Difficulty: Medium | Tests: widening, casting, integer division Read two integers via Scanner, a and b. Print a / b computed as pure integer division, and separately print the mathematically correct decimal division, side by side, so the difference is visible. Input: Two integers on one line or two lines (your choice, document it). Output: Two labeled lines: integer result, decimal result. Constraints: You must cast at least one operand to force decimal division for the second line — casting the result after the fact does not count and should be shown as a non-solution. Examples: 1) 7 3 → integer: 2, decimal: 2.3333.... 2) 10 2 → integer: 5, decimal: 5.0 (same value, good sanity check). 3) -7 2 → integer division truncates toward zero, not toward negative infinity — verify the sign of your result matches Java's actual rule. Edge cases: What happens if b is 0 for the integer division line vs. the decimal division line? (Different exceptions/behavior — identify both.) What it's testing: The specific, common bug of casting too late, plus Java's truncation-toward-zero rule for negative integer division.
* */