/*
* Difficulty: Medium–Hard | Tests: wrapper class identity vs equality Create two Integer objects both holding the value 100 using autoboxing (not new Integer(...)), and compare them with ==. Repeat with the value 200. Explain the differing results. Input: None. Output: Two boolean results, one per value, with your explanation. Constraints: Use autoboxing (Integer a = 100;), not the deprecated constructor. Examples: 1) Two Integers holding 100 → == is true. 2) Two Integers holding 200 → == is false. 3) Using .equals() instead of == for the 200 case → true regardless. Edge cases: Is this behavior specified/guaranteed by the language, or an implementation detail you should never rely on in real code? What it's testing: A genuinely famous Java trap — that autoboxed Integer identity is not reliable for comparison, and == on wrapper types compares references, not values, outside a small cached range.
* */