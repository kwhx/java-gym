/*
* Difficulty: Easy | Tests: autoboxing/unboxing mechanics Declare an Integer object initialized from a literal (triggering autoboxing), add a plain int to it (triggering unboxing for the operation), and store the result back into an int. Print all three values with labels. Input: None. Output: Three labeled lines. Constraints: Don't call .intValue() explicitly — let autoboxing/unboxing happen implicitly. Examples: 1) Integer a = 10; int b = 5; int c = a + b; → prints 10, 5, 15. 2) Same but with a reassigned mid-program to a new boxed value. 3) N/A — deterministic. Edge cases: Is there a performance/identity cost to autoboxing that wouldn't exist with plain int? (Conceptual answer, not a benchmark.) What it's testing: That boxing/unboxing is something happening to your code, not "Integer and int are basically the same thing."
* */