/*
* Difficulty: Medium–Hard | Tests: chained numeric promotion across mixed types Given the expression byte b = 10; char c = 'A'; int i = 1000; double d = 2.5; double result = b + c * i / d; — predict, step by step, the type of each intermediate sub-result as the expression evaluates (following precedence: * and / before +), and the final printed value. Then verify. Input: None (fixed expression). Output: Your step-by-step predicted types and final value, plus the verified actual output. Constraints: You must show the type at each intermediate step, not just guess the final answer. Examples: 1) c * i — what type does char promote to here, and what's the numeric result before the next step? 2) That result / d — what type governs this division? 3) Final b + (...) — confirm the whole expression's type is double because of the earlier division, regardless of b's original tiny type. Edge cases: Would the final result differ if d were 2 (an int literal) instead of 2.5? What it's testing: That numeric promotion is a per-operation, left-to-right process producing a chain of intermediate types — the mental model this whole concept has been building toward.
* */