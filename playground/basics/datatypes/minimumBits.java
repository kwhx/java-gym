/*
* Difficulty: Medium–Hard | Tests: signed representation, powers of two Given a target value V (as a long, read via Scanner) that must be representable, determine — via reasoning, not a library call — the smallest signed primitive type (byte, short, int, or long) that can hold it, accounting for the fact that signed ranges are asymmetric. Print your chosen type's name and confirm your reasoning covers both positive and negative inputs. Input: One long value (can be negative). Output: The name of the smallest sufficient type. Constraints: Reason explicitly about MAX_VALUE/MIN_VALUE boundaries; don't just eyeball digit counts. Examples: 1) V = 127 → byte suffices. 2) V = -128 → still byte (fits exactly, asymmetry matters here). 3) V = 128 → byte no longer suffices even though -128 did; needs short. Edge cases: Is there a value where the positive version needs a bigger type than the negative version of the same magnitude? What it's testing: Precise understanding of two's-complement asymmetric range, not a rough sense of "how big is big."
* */