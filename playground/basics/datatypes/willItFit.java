/*
* Difficulty: Medium–Hard | Tests: overflow-safe type selection under constraints You're told a computation will sum up to 2×10⁹ values, each up to 2×10⁹ in magnitude. State, with justification, whether int is safe for the running sum, and if not, what minimum type is. Then write a small program demonstrating the overflow using int on a scaled-down version of the same shape (two large ints near the boundary), and the corrected version using the safe type. Input: None (or two example values you choose to demonstrate with). Output: Your written justification, plus the two demonstration programs' outputs side by side. Constraints: Your scaled-down demonstration must still genuinely overflow int — arbitrary small numbers don't count as proof. Examples: 1) Two values near Integer.MAX_VALUE / 2 each, summed as int → overflows. 2) Same two values summed as long → correct. 3) A case where individual values fit in int but their product (not sum) doesn't — identify this as a distinct risk from summation. Edge cases: Does declaring the variable as long protect you if the literal/expression on the right is still computed in int before assignment? What it's testing: The single most common real-world Java bug in competitive programming — reasoning about overflow from stated constraints before writing the rest of a solution.
* */