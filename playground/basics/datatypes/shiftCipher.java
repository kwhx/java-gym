/*
* Difficulty: Medium | Tests: char arithmetic, modulo wraparound Read a single lowercase letter and an integer shift amount k. Compute the letter shifted forward by k positions in the alphabet, wrapping around from 'z' back to 'a' as needed (a single-character Caesar cipher step). No String methods. Input: One char, one int. Output: The shifted character. Constraints: Must correctly handle k larger than 26, using modulo — not a loop of 26 individual increments. Examples: 1) 'a', k=3 → 'd'. 2) 'y', k=3 → 'b' (wraps around). 3) 'a', k=52 → 'a' (two full wraps). Edge cases: What does your formula do if k is negative (shifting backward)? Does it still wrap correctly, or does it produce a negative intermediate that breaks the char cast? What it's testing: Real modulo-arithmetic-on-char reasoning, the exact building block used constantly in actual CP string/cipher problems.
* */