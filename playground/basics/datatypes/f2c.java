/*
* Difficulty: Easy | Tests: literal typing + operator/promotion interaction Read an int Fahrenheit temperature via Scanner. Convert to Celsius using the formula (F - 32) * 5 / 9 and print the result as a double with correct precision (not truncated to an integer). Input: One line, one integer. Output: One line, the Celsius value with decimal precision. Constraints: You must reason about where in the expression a double needs to appear — adding .0 in the wrong place still truncates. Examples: 1) Input 212 → 100.0. 2) Input 32 → 0.0. 3) Input 98 → a non-round decimal value, not 36.0 (verify your formula doesn't accidentally integer-divide first). Edge cases: Does (F - 32) * 5 / 9 behave differently from (F - 32) * 5.0 / 9? Why? What it's testing: That you understand promotion happens per-operation, left to right, not "once the whole expression touches a double."
* */