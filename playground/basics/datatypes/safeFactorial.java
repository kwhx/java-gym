/*
* Difficulty: Medium–Hard | Tests: literals, casting, promotion, overflow reasoning combined Read an integer N (guaranteed 0 ≤ N ≤ 20) and compute N!. Choose your variable type deliberately based on how large 20! actually is (reason about it, don't guess), and if N! would exceed that type's range within the given constraint, say so explicitly rather than silently producing a wrong wrapped value. Input: One integer, 0 ≤ N ≤ 20. Output: Either the correct factorial value, or an explicit statement that it doesn't fit your chosen type — decide which is actually true for this constraint before writing the "doesn't fit" branch. Constraints: Must not simply default to long "to be safe" without justifying that the constraint actually demands it. Examples: 1) N=5 → 120. 2) N=20 → a large but specific long-range value — verify it doesn't silently overflow int first during the multiplication chain. 3) N=0 → 1 (identify why this isn't a special case in your loop, just the correct base value). Edge cases: If your accumulator starts as int and you multiply into it before ever assigning to a long variable, does declaring the final variable as long save you? What it's testing: Whether overflow-safety reasoning survives being embedded inside an actual small algorithm, not just isolated trap exercises.
* */