/*
* Difficulty: Medium | Tests: overflow behavior Given: int x = Integer.MAX_VALUE; x = x + 1; — predict the printed value of x before running it, then verify. Repeat the exercise for byte overflowing past 127. Input: None. Output: Your predicted values plus the actual printed values, side by side. Constraints: No exception is thrown — that's the point; note explicitly that Java integer overflow is silent, unlike some languages. Examples: 1) int overflow wraps to Integer.MIN_VALUE. 2) byte 127 + 1 wraps to -128. 3) long doesn't overflow at the same boundary as int — confirm the same int overflow doesn't happen if you'd used long for the same literal value. Edge cases: Does multiplying two large ints that individually fit in range still risk overflow? What it's testing: That overflow is silent wraparound in Java, not a caught error — a dangerous default to be unaware of.
* */