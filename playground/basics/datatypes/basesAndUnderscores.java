/*
* Difficulty: Trivial | Tests: numeric literal syntax (binary/octal/hex/decimal, underscores) Declare four int variables holding the same value, written as: a plain decimal literal, a binary literal (0b...), an octal literal (0...), and a hex literal (0x...). Also declare one large decimal literal using underscores for readability. Print all five values and confirm the first four are equal. Input: None. Output: 5 printed values; the first four numerically identical. Constraints: Pick a value that's easy to hand-verify across bases (e.g. 26). Examples: 1) 26, 0b11010, 032, 0x1A all print 26. 2) 1_000_000 prints 1000000. 3) Placing an underscore directly after 0x or at the very start/end of a literal — try it and note the compiler's reaction. Edge cases: Can underscores appear adjacent to the decimal point in a floating-point literal? What it's testing: That "numeric literal" is a richer concept than decimal digits.
* */