/*
* Problem statement: Using only System.out.print and System.out.println calls (no string concatenation, no loops), produce this exact output (note where lines break and where they don't):

Name: Ada
Language: JavaVersion: 1

Input format: None. Output format: Must match exactly, including the line break positions. Constraints: Use at least one print and at least one println.

Examples:

Correct sequence of calls → the block above, verbatim.
Swapping one print for println → produces a visibly different (wrong) line-break pattern — you should be able to predict this before running it.
Using only println for everything → also wrong; identify why.

Edge cases to consider: Does print add any trailing character at all? What exactly does println add, and when? What this tests: Precise mental model of what each call does to the output stream, not just "prints stuff."
* */