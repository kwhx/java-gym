/*
* Difficulty: Level 5 Concepts tested: Correct type choice, overflow avoidance, boolean composition, integer/float precision — combined Problem statement: A store computes a final price from: basePrice (double), quantity (int, can be large), discountPercent (int, 0–100), and a isBulk flag (1 if quantity ≥ 1000, else 0, but you must derive this flag yourself from quantity, not trust an input value — actually, don't read it as input at all, compute it). Bulk orders get an additional flat 5% off, applied after the percent discount, but the final price can never go below 0. Read basePrice quantity discountPercent. Print the final total price (basePrice * quantity, discounted), formatted as a plain double, with correct handling of large quantity values that could overflow int arithmetic if you multiply carelessly. Input format: basePrice(double) quantity(int) discountPercent(int), space-separated. Output format: One double value (final total). Constraints: 0 ≤ basePrice ≤ 10^5, 0 ≤ quantity ≤ 2×10^9 (exceeds int range — choose your types accordingly), 0 ≤ discountPercent ≤ 100 Examples:

Input: 10.0 500 10 → 4500.0 (500 units × $10, 10% off, not bulk)
Input: 10.0 1000 10 → 4275.0 (1000 units × $10 = 10000, 10% off = 9000, bulk extra 5% off = 8550... recheck your own arithmetic carefully; this is intentionally dense)
Input: 5.0 2000000000 100 → 0.0 (100% off floors it to zero regardless of bulk/quantity) Important edge cases: quantity large enough that quantity alone doesn't fit comfortably if you accidentally do intermediate int math instead of double/long; discountPercent = 100 producing exactly 0, not a negative number. What it specifically tests: End-to-end operator discipline under a realistic multi-step calculation — correct type selection for large values, correct order of discount application, and a floor check — without any loop, as a bridge into Loops.
* */