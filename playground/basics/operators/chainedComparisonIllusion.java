/*
* Difficulty: Level 3 Concepts tested: Precedence/associativity trap, boolean vs. numeric confusion Problem statement: A beginner writes boolean result = 1 < 2 < 3; hoping to check "is 2 strictly between 1 and 3." Explain in one or two sentences why this either fails to compile or (for a variant like 5 > 3 > 1) compiles but computes something other than the intended mathematical chain — trace exactly what each sub-expression evaluates to and what type it produces at each step. Then write the correct single boolean expression for "is b strictly between a and c" using proper Java operators, and show it evaluated on three test triples of your own choosing. Input format: None (reasoning + short example construction). Output format: Your explanation, the corrected expression, and its result on 3 self-chosen triples. Constraints: N/A Examples: (illustrative only, not literal I/O) — 1 < 2 < 3 fails to compile because 1 < 2 produces a boolean, and boolean < 3 is not a valid comparison; 5 > 3 > 1 also fails for the same structural reason (comparison operators don't accept a boolean operand). Important edge cases: Recognize that this trap is really about type, not just precedence — the left comparison produces a boolean, and the second </> has no valid boolean-vs-int overload, which is why it's a compile-time error rather than a silently wrong runtime value. What it specifically tests: Whether you understand that Java comparison operators are strictly binary and non-chaining, unlike math notation, and that this is a type error, not just "wrong precedence."
* */