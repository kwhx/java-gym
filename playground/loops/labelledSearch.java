/*
* Difficulty: Level 5 Concepts tested: Labeled break, nested-loop search, compound boolean/arithmetic exit condition, efficiency reasoning Problem statement: Read one integer limit. Search pairs (i, j) with 1 ≤ i ≤ limit and i < j ≤ limit (using two nested loops, no arrays) for the first pair, in row-major order (increasing i, then increasing j within each i), satisfying all of: (i + j) % 7 == 0, (i * j) is even, and (i XOR j) (bitwise) is greater than i. The moment such a pair is found, stop searching entirely using a labeled break (not a flag variable checked every iteration) and print i j. If no pair exists within the bound, print NONE. Additionally, add an inner-loop optimization: skip (via continue, not by restructuring the loop bounds) any j where (i + j) % 7 != 0 before evaluating the more expensive XOR condition, since the modulo check is cheaper to fail fast on. Input format: One integer limit. Output format: i j on one line, or NONE. Constraints: 2 ≤ limit ≤ 5000 Examples:

Input: 10 → work through the search order by hand to find the actual first qualifying pair — don't guess.
Input: 2 → NONE (too small a range for any pair satisfying (i+j)%7==0 with j ≤ 2)
Input: 20 → some valid pair — verify independently. Important edge cases: limit small enough that no pair satisfies the modulo condition at all (must print NONE, not crash or loop forever); confirming the labeled break exits both loops immediately upon the first match, rather than merely skipping to the next i. What it specifically tests: Combining labeled break for efficient full-exit search, continue as a genuine performance/readability optimization (not just a break-avoidance habit), and a nontrivial compound condition mixing modulo, parity, and bitwise XOR — the densest single integration point in this curriculum.
* */