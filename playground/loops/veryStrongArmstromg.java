/*
* Difficulty: Level 5 Concepts tested: Nested loop-based exponentiation, digit manipulation, overflow trap combined Problem statement: Read one integer n (positive). An Armstrong number is one where the sum of its digits, each raised to the power of the total digit count of n, equals n itself (e.g. 153 = 1³ + 5³ + 3³). You must: (1) first determine the digit count using a loop (not by converting to String), (2) then compute each digit's contribution using a loop-based power (no Math.pow, which returns double and risks precision issues here), (3) accumulate the sum, watching for intermediate overflow if n is large enough that even a non-Armstrong candidate's digit-power sum could exceed int range. Print true/false. Input format: One integer n. Output format: true or false. Constraints: 1 ≤ n ≤ 10^9 Examples:

Input: 153 → true
Input: 154 → false
Input: 9474 → true Important edge cases: Single-digit numbers (every single-digit number is trivially an Armstrong number, since d^1 = d — verify your digit-count loop returns 1 correctly for these, not 0); a large n where accumulating digit powers could overflow int before you've even finished summing, requiring long for the accumulator even though the final comparison is against an int-range n. What it specifically tests: Chaining three loop-based sub-computations correctly (digit count → per-digit power via loop → accumulation), plus real overflow-awareness in an intermediate accumulator rather than just the final answer — this is where sloppy type choices from earlier levels would resurface as silent wrong answers.
* */