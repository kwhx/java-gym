/*
* Difficulty: Level 4 Concepts tested: State-tracking simulation loop Problem statement: Read a starting balance balance (int) and a number of months T. For each month (1 through T, in order): interest of exactly 2% is added (integer cents, truncate any fractional cent — do not round), then a fixed withdrawal of withdrawAmount (read once, applies every month) is subtracted. If at any point the balance would go negative, stop the simulation immediately (do not apply that month's withdrawal — leave the balance as it was after interest that month) and report the month number it stopped at. If it completes all T months without going negative, report -1 for the stop month. Input format: Three integers balance T withdrawAmount. Output format: Two lines: final balance, and the stop month (or -1). Constraints: 0 ≤ balance ≤ 10^7, 1 ≤ T ≤ 1000, 0 ≤ withdrawAmount ≤ 10^7 Examples:

Input: 1000 3 50 → interest/withdrawal traced month by month; verify by hand before trusting any assumed answer.
Input: 100 5 1000 → should stop very early (month 1), since a huge withdrawal on a small balance goes negative immediately.
Input: 0 3 0 → completes all 3 months, balance stays 0, stop month -1. Important edge cases: Withdrawal that would make the balance exactly 0 (should be allowed — only going below zero triggers the stop); T months completing without ever going negative. What it specifically tests: Correct state-tracking across iterations (each month's balance depends on the previous), a within-loop early-exit condition (break with if), and precise int-only interest computation (truncation, not rounding).
* */